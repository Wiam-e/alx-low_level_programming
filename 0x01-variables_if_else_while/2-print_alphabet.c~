#include <stdio.h>
/**
*main - a program that prints the alphabet in lowercase
*Return: 0 (Success)
*/
int main(void)
{
int i;
char alpha[26] = "abcdefghijklmnopqrstuvwxyz";
for (i = 0; i<26; i++)
{
putchar(alpha[i]);
}
putchar("\n");
return (0);
}
